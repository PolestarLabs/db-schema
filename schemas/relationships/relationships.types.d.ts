// import type { Rarity, Currency } from '../../types/generics';

/** Front-facing `Relationships` type — clean unions for API/Bot consumption. */
export interface RelationshipsData {
  // TODO: mirror your schema fields but with proper union types
  // id: string;
  // rarity: Rarity;
}
